x=3.14
print(type(x))
#print(type())可以查看数据类型
#不可变数据（3 个）：Number（数字）、String（字符串）、Tuple（元组）；
#可变数据（3 个）：List（列表）、Dictionary（字典）、Set（集合）。
#此外还可以用 isinstance 来判断：>>> a = 111 >>> isinstance(a, int) 输出True
